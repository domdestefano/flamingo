import type { SidebarsConfig } from '@docusaurus/plugin-content-docs';

// Two independent sidebars so "Components" and "Guidelines" are separate
// navbar sections. `autogenerated` means the sidebar mirrors the folder
// structure under docs/ — add a new .mdx file and it appears automatically,
// so you rarely touch this file again.

const sidebars: SidebarsConfig = {
  componentsSidebar: [
    {
      type: 'autogenerated',
      dirName: 'components',
    },
  ],
  guidelinesSidebar: [
    {
      type: 'autogenerated',
      dirName: 'guidelines',
    },
  ],
};

export default sidebars;
