---
title: Overview
sidebar_position: 1
status: stable
platforms: [web, ios, android]
category: buttons
figma_node: '40001021:30260'
used_in: [checkout, order-tracking, rider-onboarding]
tags: [button, tertiary, action, cta, interactive]
---

import ComponentImage from '@site/src/components/ComponentDocs/ComponentImage';
import ComponentTabs from '@site/src/components/ComponentDocs/ComponentTabs';
import { StatusBadge, AvailabilityTable } from '@site/src/components/ComponentDocs/shared';

# Tertiary button <StatusBadge status="stable" />

Indicate the last, least important, or most repetitive action on a screen.

<ComponentTabs basePath="/components/buttons/tertiary-button" />

<AvailabilityTable figma="available" web="available" ios="available" android="available" specs="available" />

## Component

<ComponentImage src="/img/components/tertiary-button/tertiary-button-overview.png" alt="Tertiary button states: active, pressed, inactive, loading, and icon left/right" />

1. Active, pressed, inactive, and loading states
2. Icon left and right

Use the tertiary button for **low emphasis** — for the last or least important actions on a screen.
It's also the right choice for **repetitive actions**, when icons alone can't clearly communicate
what the action does.

## Properties

| Property | Details |
| --- | --- |
| State | **Active**: the button is clickable. **Pressed**: the button is being pressed. **Inactive**: the button is not interactive and not clickable. **Loading**: the button is showing that an action is being triggered. |
| Icon (position) | **No icon**. **Left**: use when you want to draw the user's attention to the icon first, or when the action represents a backward flow. **Right**: use when the action represents a forward flow or the next step. Avoid using an icon in tertiary buttons unless explicitly needed. |

:::warning Size deprecated (Nov 2025)
The tertiary button used to come in 3 sizes — Small, Medium, Large — depending on the prominence
designers wanted to give the action and the size of the element it refers to. As of Nov 2025, the
different sizes are deprecated — use the **Medium** size in Figma and code.
:::

## Anatomy

<ComponentImage src="/img/components/tertiary-button/tertiary-button-anatomy.png" alt="Tertiary button anatomy: icon and text" />

1. Icon
2. Text

Unlike Primary and Secondary button, the tertiary button has no background — it's text (plus an
optional icon), styled to read as clickable without competing visually with higher-emphasis
actions on the same screen.

### Clickable area

The tertiary button's tappable area is larger than its visible text, so it still meets the
standard touch target size even though the label itself is small — don't shrink the hit area to
match the text visually.

<ComponentImage src="/img/components/tertiary-button/tertiary-button-clickable-area.png" alt="Tertiary button clickable area, larger than the visible text" width={220} />
