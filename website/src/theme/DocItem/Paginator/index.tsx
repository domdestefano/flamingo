import type {ReactNode} from 'react';

// Ejected from @docusaurus/theme-classic and reduced to a no-op — the site
// doesn't want prev/next navigation at the bottom of doc pages.
export default function DocItemPaginator(): ReactNode {
  return null;
}
